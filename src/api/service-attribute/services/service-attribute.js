'use strict';

/**
 * service-attribute service
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::service-attribute.service-attribute');
