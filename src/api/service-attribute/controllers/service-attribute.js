'use strict';

/**
 * service-attribute controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::service-attribute.service-attribute');
